# Google Gemini API Key (optional - if not provided, will use local Ollama models)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Chroma persist directory (optional - defaults to "chroma_store")
# This is where vector embeddings will be stored for persistence
CHROMA_PERSIST_DIR=chroma_store
